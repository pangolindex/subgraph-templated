{
  "name": "subgraph-templated",
  "version": "0.0.1",
  "license": "MIT",
  "scripts": {
    "compile": "hardhat compile",
    "codegen": "graph codegen",
    "build": "graph build",
    "create-local": "graph create --node http://localhost:8020/ templated",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 templated",
    "remove-local": "graph remove --node http://localhost:8020/ templated"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "0.33.0",
    "@graphprotocol/graph-ts": "0.27.0",
    "@hashgraph/sdk": "^2.19.0",
    "axios": "^1.2.1",
    "dotenv": "^16.0.3",
    "hardhat": "^2.12.4",
    "web3": "^1.8.1"
  }
}
